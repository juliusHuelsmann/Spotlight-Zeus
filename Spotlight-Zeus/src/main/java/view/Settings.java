package view;

import view.util.WindowShape;

public class Settings extends WindowShape {

  public Settings() {
    super();
    super.setSize(400, 250);
    super.setVisible(true);

    // About, Update, version, launch on startup
    //
    // The way text is selected (DELPHI FOR NOW)
    //
    // The information that are displayed (wikipeida, etc. ATHENE)
    //
    
    
  }
  public static void main(String[] args) {
    new Settings();
    
  }
}
